services:
  mcp_server:
    build: ./mcp_server
    environment:
      - MCP_API_KEY=${MCP_API_KEY}
      - GOOGLE_CALENDAR_ID=${GOOGLE_CALENDAR_ID}
      - GOOGLE_CREDENTIALS=${GOOGLE_CREDENTIALS}
    ports:
      - "8080:8080"
